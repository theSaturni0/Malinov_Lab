#pragma once

#include <string>
#include <vector>

namespace saturnLib {
    bool validNum(std::string num);
    std::vector<std::string> numString(std::string num);
    bool validNumString(std::vector<std::string> vectorNumbers);
}